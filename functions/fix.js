const fs = require('fs'); let c = fs.readFileSync('src/index.ts', 'utf8'); c = c.replace(/if \(\!privateKey \|\| \!privateKey\.startsWith\('0x'\)\) \{[\s\S]*?throw new HttpsError\('internal', "Faucet is currently offline."\);\s*\}/m, 'if (!privateKey) { throw new HttpsError("internal", "Offline"); } if (!privateKey.startsWith("0x")) { privateKey = "0x" + privateKey; }'); fs.writeFileSync('src/index.ts', c);
